# ComfyUI Arguments for RTX 6000 Pro Blackwell (48GB VRAM)
# These are optimized for professional workstation GPU

# Performance optimizations
--fast

# CRITICAL: Disable xformers - not compatible with Blackwell (sm_120)
# xformers only supports up to compute capability 9.0 (Ada/Hopper)
--disable-xformers

# Use PyTorch's native scaled dot product attention instead
--use-pytorch-cross-attention

# VRAM management - reserve 0.5GB for system
--reserve-vram 0.5

# Use high VRAM mode (48GB is plenty)
--highvram

# Preview generation
--preview-method auto
